---
name: build-and-push

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
      
jobs:
  release-image:
    runs-on: [deploy, uopve]
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Build and push
        run: ./buildandpush.sh
